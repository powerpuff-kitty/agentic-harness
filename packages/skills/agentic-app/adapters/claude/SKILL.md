# Claude Code Adapter

Use the canonical agentic-app procedure at `../../SKILL.md` for INIT, UPGRADE, and AUDIT work.

Keep Claude-specific behavior here only when it cannot be represented in the shared skill.