# Components

For each durable component document anatomy, API/variants, size rules, states, interaction behavior, accessibility semantics, responsive behavior, token dependencies, content constraints, examples, and anti-patterns.

Do not duplicate global token definitions inside individual component docs.
