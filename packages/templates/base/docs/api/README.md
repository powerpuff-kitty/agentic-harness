# API and Event Contracts

Document public/internal contract boundaries, authentication/authorization, validation, errors, pagination, idempotency, versioning, compatibility, rate limits, events, and deprecation policy.

Public contract changes should have explicit migration/compatibility reasoning and tests.
