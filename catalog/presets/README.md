# Presets

Presets are named machine-readable compositions that select a complete catalog boilerplate plus packs, policies, skills, and optionally a profile. They reduce setup choices without creating a separate variant for every technology combination.
