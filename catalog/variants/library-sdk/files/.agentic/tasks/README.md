# Tasks

Tasks hold active coordination state, not durable project truth. Put current work in `active/` and completed or cancelled records in `archive/`. Prefer the repository's issue tracker when it is the authoritative task system; use this directory only when local agent-readable state is needed.
