# Installed policies

Policies are mandatory must/must-not rules. They take precedence over current project truth unless an explicitly authorized exception mechanism says otherwise. Resolved versions and checksums belong in `../lock.json`.
