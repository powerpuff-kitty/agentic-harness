# Monorepo testing

Define changed-project detection, package-level tests, integration contracts, end-to-end coverage, cache correctness, affected graph validation, and full-repository fallback gates.
