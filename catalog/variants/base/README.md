# Base boilerplate

Minimal agent-native project contract. It keeps only the normal project README and `AGENTS.md` at the root; canonical project context lives under `.agentic/`, and reusable procedures live under `.agents/skills/`.

The `files/` directory is copied into a target project. Other variants are complete materialized derivatives of this structure so they remain directly inspectable and deterministic.
