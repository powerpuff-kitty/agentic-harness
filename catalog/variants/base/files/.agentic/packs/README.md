# Installed packs

Installed reusable domain knowledge and constraints are copied here. The resolved versions and checksums belong in `../lock.json`; the selected pack names belong in `../manifest.yaml`.
