# Architecture Decision Records

ADRs preserve why consequential choices were made. They are immutable history: supersede an accepted ADR with a new record rather than rewriting its decision.

Statuses: `proposed`, `accepted`, `rejected`, `deprecated`, `superseded`.

Use `ADR-NNN-short-kebab-title.md`, update `index.yaml`, and reflect accepted current state in the relevant canonical truth file.
