# Tasks

Use repository issues as the primary task system. This directory is reserved for local agent-readable execution state when an issue cannot represent the necessary context. Durable outcomes belong in current truth or ADRs.
