# Operations

The static and agent repositories are distributed through Git. The CLI is distributed as source and precompiled release binaries. Operational concerns include source pin updates, release artifact integrity, failed release recovery, compromised source response, and deprecation communication.
