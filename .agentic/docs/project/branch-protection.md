# Recommended repository protection

Require pull requests, successful repository-specific validation, conversation resolution, and protected release tags. Disallow force pushes on `main`. Prefer signed release tags and least-privilege workflow permissions. Repository-host settings must be verified separately because files alone cannot enforce them.
