# Synthetic reading list

This is a filled project-context example for catalog issue #84 and a starting scenario for #86. It contains no application implementation. Choices below are stipulated for this synthetic scenario, not evidence of a shipped product.

One person keeps a temporary reading list in a browser tab. The intended stack is Vue and TypeScript, with no account, backend, persistence or external service. See [the context map](.agentic/README.md).

Context is authored; no modules or skills are installed. The lock has unresolved source identities and empty checksums intentionally. Tests, browser support, host delivery and runtime behavior are unverified. Do not reuse this fixture's lock as installation provenance or claim it was generated by the CLI.

The complete #86 example must add working source, approved checks, seeded violations, exact source pins and executed evidence. This context-only fixture does not satisfy that integration ticket.
