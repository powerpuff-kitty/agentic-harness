# Release

Verify versioning, changelog, compatibility, migrations, tests, security checks, artifacts, rollback path, observability, documentation, and known risks. Produce immutable build artifacts from a reviewed commit, checksums/signatures when appropriate, and release notes focused on user-visible change. Never publish from an unverified dirty workspace.
