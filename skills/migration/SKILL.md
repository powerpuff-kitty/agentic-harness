# Migration

Plan and execute data, schema, API, framework, or infrastructure migrations safely.

Discover current state and invariants; define target state; identify compatibility windows; create reversible stages; design backfill and validation; define rollback/roll-forward strategy; test on representative data; monitor during rollout; remove compatibility code only after verification. For destructive or irreversible steps require explicit approval.
