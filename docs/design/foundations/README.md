# Foundations

Document grid, containers, density, breakpoints, typography foundations, input/interaction fundamentals, and accessibility rules. Keep raw token scales in `../tokens/` and reusable page composition in `../layouts/`.
