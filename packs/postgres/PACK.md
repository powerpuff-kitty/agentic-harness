# postgres

Use when PostgreSQL is a material part of the system.

Document schema ownership, constraints/invariants, migrations, indexes, transaction boundaries, isolation assumptions, query performance, connection/pooling policy, backup/recovery expectations, retention, and sensitive-data handling.

Prefer database constraints for invariants that must hold regardless of application path.
