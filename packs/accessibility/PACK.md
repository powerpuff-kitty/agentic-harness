# Accessibility Pack

Use for user-facing interfaces.

Target WCAG 2.2 AA unless project requirements say otherwise. Require semantic HTML, keyboard operation, visible focus, accessible names, appropriate contrast, reduced-motion support, form error association, screen-reader-compatible status updates, and automated plus manual checks for critical flows. Accessibility is a product requirement, not a final QA pass.
