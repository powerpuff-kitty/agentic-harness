# Authentication Pack

Use when the product has identities, sessions, credentials, API keys, SSO, or authorization.

Separate authentication from authorization. Require secure session lifecycle, rotation/revocation, rate limiting, recovery flows, MFA/passkeys where appropriate, least privilege, explicit tenant boundaries, and audit trails for privileged actions. Never invent auth behavior from framework defaults without verification.
