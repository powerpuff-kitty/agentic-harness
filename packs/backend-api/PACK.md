# backend-api

Use for HTTP/RPC/event-driven backend services.

Document API conventions, authentication/authorization boundaries, validation, errors, idempotency, pagination, versioning, compatibility, rate limits, events/jobs, data ownership, and observability.

Public contract changes require a plan and compatibility review.
