# Presets

Presets are named machine-readable compositions of a root boilerplate plus reusable modules and agent skills. They reduce setup choices without creating near-duplicate boilerplates.

Use the `boilerplate` field to select one of the root catalog directories (`base`, `web-app`, `backend-api`, `saas`, `monorepo`, or `library-sdk`).
