# Monorepo boundaries

Document apps, services, packages, dependency direction, ownership, shared-code policy, build/test scopes, release units, and where nested AGENTS.md routers are justified.

Prefer local agent instructions only when a subtree has materially different commands, constraints, or source-of-truth documents.
