# Design Anti-patterns

Record recurring rejected outcomes and why they fail. Prefer specific, testable guidance over taste statements.

Examples: raw colors bypassing semantic tokens, arbitrary one-off spacing, inaccessible focus states, inconsistent page widths, destructive actions without confirmation, layout rules embedded only in screenshots.
